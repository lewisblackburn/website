'use client'

import { buttonVariants, Link } from '@zxffo/ui'

const GoHomepage = () => {
  return (
    <Link href='/' className={buttonVariants()}>
      Go homepage
    </Link>
  )
}

export default GoHomepage
